# 安全政策

## 这个项目的攻击面

proto-harness 是**本地静态工具**：Node 脚本 + 规则文档 + 静态 HTML 原型。它不包含服务端、不联网、不处理用户数据、不提供部署或上传能力（这是刻意的设计边界，见 [`CHANGELOG.md`](CHANGELOG.md#设计取舍)）。

因此真实的安全风险集中在三类：

1. **脚本的路径与命令处理** —— `scripts/*` 会遍历目录、读写登记文件、调用 `git status`。若某个路径参数可被外部内容影响，存在目录逃逸或注入面的可能。
2. **本地静态服务的暴露范围** —— `npm run serve:local` 默认绑定 `127.0.0.1`。若你自行改成对外监听，原型内容会对局域网可见。
3. **示例页面的 XSS 面** —— 需求抽屉会把 `standards/*` 与页面内 `window.DocsData` 的结构化内容渲染成 HTML。接入方若把这些内容接到真实外部输入上，就超出了本仓库的默认假设。

## 报告漏洞

请通过仓库页面「安全」标签页里的私密漏洞上报入口提交，**不要开公开议题**。该入口由代码托管平台提供，本文件刻意不写死链接地址，以免仓库迁移或改名后失效。

若私密上报通道在你的环境中不可用，请开一个标题不含细节的议题（例如「安全问题咨询」），正文只写「请提供联系邮箱」，后续通过私信沟通。


维护者会在可行范围内回复确认。本仓库是个人维护的开源项目，没有 SLA，也没有安全响应团队。

## 不在范围内

- 要求处理部署、鉴权、服务器加固 —— 本仓库不提供这些能力，也不打算提供。
- 由接入方自行改坏的用法（例如把 `serve:local` 对外监听后产生的内容泄露）。
- 依赖上游项目（Font Awesome、Tailwind、`docx`）自身的漏洞，请向对应上游报告。

## 支持版本

| 版本 | 是否支持 |
| --- | --- |
| 0.1.x | ✅ |
| < 0.1 | ❌ 首个公开版本之前的历史不追补修复 |
