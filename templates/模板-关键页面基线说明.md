# 页面名称 - 页面基线说明

## 页面用途

- 页面目标：
- 所属平台：
- 页面类型：
- 开发路径：
- 复刻来源（clone-source）：
- 本次变更类型（change-type）：safe-add / safe-reorder / visual-tightening / breaking-change

## 受保护字段

| 字段 | 说明 | 默认是否允许改动 |
|---|---|---|
| 示例字段 | 现有字段说明 | 否 |

## 受保护操作

| 操作 | 说明 | 默认是否允许改动 |
|---|---|---|
| 示例按钮 | 现有按钮行为 | 否 |

## 受保护状态

| 状态 | 说明 | 默认是否允许改动 |
|---|---|---|
| 示例状态 | 现有状态逻辑 | 否 |

## 允许新增项

- 可新增字段：
- 可新增标签：
- 可新增说明区：
- 可接受的局部重排：

## 未明确要求时不得改动

- 不删除既有字段
- 不删除既有操作
- 不修改既有状态语义
- 不修改既有跳转链路
- 不改动未被本次指令覆盖的功能
- `safe-add` 默认不得顺手新增筛选器、统计条、快捷入口、说明卡片或新的交互模块
