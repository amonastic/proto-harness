# proto-harness AI 主规则

本文件是仓库的**唯一主入口**，负责最高优先级规则、执行状态判定、开工闸门、交付边界与高风险红线。专项细则全部下放到 `harness/`、`DEVELOPMENT.md` 与 `standards/`，本文件不重复维护细节。

## 对话称呼握手规则

- 所有 AI 每次回复用户时，输出正文必须以「山」开头。
- 该规则只用于让用户快速判断当前回复是否命中本仓库可见握手词；若回复不是以「山」开头，视为未通过 Harness 握手。
- 「山」不是合规证明，不代表本轮已经读取当前文件，不代表后续动作遵循 Harness，也不代表允许修改文件；真正的合规证明只能来自本轮状态判定、当前规则读取证据、授权原文、修改范围、禁止范围和验证结果。
- 该规则适用于分析、讨论、执行、排障、治理、最终回复和中途状态更新；不因任务类型、模型、工具环境或是否修改文件而豁免。
- 不得把「山」放在第二句、标题后、代码块中或解释性前缀后；必须直接作为对用户可见回复正文的开头。
- 当本文件、Harness 或规则入口刚被修改、回退或被用户质疑时，下一次判断或执行必须重新读取当前本地规则文件，并说明当前生效入口。

## 文档地图与读取顺序

| 层 | 文件 | 职责 |
| --- | --- | --- |
| 主入口 | `AGENTS.md`（本文件） | 最高优先级规则、状态判定、闸门、红线 |
| 执行分卷 | `harness/00-执行总入口.md`、`harness/01-强制闸门.md` | 每轮默认读取的最小链路 |
| 专项分卷 | `harness/02-纠偏与接力.md`、`03-任务契约.md`、`04-规则准入与治理.md`、`05-验证与交付.md`、`10-页面与抽屉执行.md`、`20-协作Prompt模板.md` | 按任务路由读取（路由表见 `harness/README.md`） |
| 角色正文 | `harness/roles/*.md` | 命中角色信号时读取 |
| 实现基线 | `DEVELOPMENT.md` | 页面结构、抽屉、弹层、静态原型、复刻改造 |
| 结构化标准 | `standards/*.json`、`standards/*.md` | 机检口径与专项约束 |
| 授权口径 | `standards/授权词表.md` | 判断用户是否给出明确执行授权 |
| 工作流 skills | `docs/skills/` | 诊断、评审、拆解、收尾等可复用流程 |

## 会话开始

- 先读本文件，再按任务分流读取 `harness/` 与 `standards/`。
- 会话开始时读一次 `memory/MEMORY.md`（如存在）；记忆只是事实与决策指针，不是规则，与本文件、Harness 或 `standards/*` 冲突时规则优先，记忆不构成执行授权。
- **每一条用户新消息都必须重新判定状态**；最新用户消息覆盖旧计划、旧授权和上一轮执行惯性。上一轮出现过执行授权，不得自动延续到本轮。

## 执行状态判定

| 状态 | 触发口径 | 允许动作 |
| --- | --- | --- |
| 分析执行态 | 「看看」「帮我看下」「审一下」「查一下」「分析一下」「有没有问题」 | 必须读取文件、检索依据、对照规则，输出结论、风险、建议与待确认项；不得修改文件 |
| 纯讨论态 | 「怎么看」「方案是什么」「是否需要」「对比一下」且未要求读取仓库或产出交付物 | 只输出理解、影响范围、方案、风险、待确认项 |
| 文件执行态 | 命中 `standards/授权词表.md` 定义的明确授权 | 按开工声明与 preflight 修改文件 |
| 排障态 | 「坏了」「点不了」「报错」「机检失败」「页面异常」，以及「没变」「还是一样」「没效果」 | 先复现和定位，再最小修改；未完成诊断前不得凭原理解继续改 |
| 治理态 | 「整理规则」「同步文档」「收口」「新人上手」 | 梳理资料层级，避免新增平行规则 |

疑问句式（「能不能」「可不可以」「是否可行」）默认进入分析执行态：只输出分析与方案，待用户给出明确执行授权后再进入文件执行态。

用户未明确进入文件执行态时，不得修改代码、文档、脚本或删除文件。用户要求「输出一个 md / 落报告 / 生成清单」时，默认进入文件执行态，但只允许新增或修改该交付文档，不得顺手改现有规则正文。

## 开工默认流程（文件执行态）

动手前先声明：

1. 本轮准备修改的文件
2. 本轮禁止修改的文件或范围
3. 本轮已读取的规则文件（每个必读文件用一句话说明它对本轮的具体约束）
4. 本轮复用的成熟页面或组件（如有）
5. 本轮执行依据
6. 本轮是否允许新增交互、提示、测试项、验收项

并满足：

- 必须引用**本轮授权原文**：逐字摘出用户本轮明确要求修改或交付的句子；引用不出明确授权时不得修改文件。
- 必须检查 `git status --short` 或等效差异来源，区分本轮改动、既有非本轮改动、未跟踪文件与禁止触碰范围；不得回滚、覆盖或顺手提交非本轮改动。
- 修改页面、抽屉、弹窗、公共组件前，必须先声明复用的成熟实现（clone-source）；未声明不得动手。
- 输入包含接力材料、上一位 AI 结论或历史审阅时，必须先完成材料分层，未分层不得修改文件。

## 高风险红线

### T0：不做未经授权的重构

- 未经用户明确要求「重构 / 全量替换 / 删除重写 / 整体重做」，不得大片修改、替换或删除既有逻辑、文档、页面结构、导航结构、脚本规则或已登记页面。
- 最小增量授权下，默认只做最小必要修改：保留原内容，在原位置追加、修正或标注。
- 确需大范围调整时，先列出涉及文件、章节、将删除或替换的内容、原因与风险，等用户确认后执行。

### T-1：历史快照不可污染

- 历史迭代页（早于当前最新迭代）不得直接引用仍在持续演进的源页面，必须引用冻结快照。
- 快照必须声明快照来源、归属迭代与快照日期（`snapshot-of` / `snapshot-iteration` / `snapshot-date`）。
- 快照不是只复制 HTML：页面私有的 JS/CSS、需求抽屉数据、图片、模板等影响展示与说明口径的依赖必须一起冻结。
- 快照不得引用公共可变需求数据源（如仍会更新的模块级 `docs.js`）。
- 修改迭代索引、快照或源页面后，必须检查是否造成历史迭代引用活页、快照缺依赖或菜单数量不一致；发现污染先报告，未修复前不得给出「已收口」结论。

### 未经明确指令不得直接执行

- 用户处于纯讨论、评审、发散、对比方案阶段时，不得直接修改代码、文档、脚本或删除文件。
- 无明确依据时，不得把猜测写成事实；只能标记为「待确认项 / 推断 / 现有文档未确认」。

## 执行依据规则

输出结论、字段、页面规则、交互逻辑、接口参数、异常场景、改动建议时，必须说明依据。合法依据仅允许来自：

- 用户本轮明确要求
- 仓库内现有代码、页面、设计稿
- 需求文档、规则文档、接口文档
- 用户确认过的成熟页面复用来源

未在代码、文档或页面中确认的事实，不得直接落结论。

## 页面调整契约闸门

- 用户是最高维度审阅者，不负责替 AI 拆字段、拆页面、拆布局；当需求说明或方案可行但尚未具象到页面级差异时，AI 必须主动产出页面调整契约。
- 「需求说明 OK / 方案 OK」不等于可以直接改页面；只有页面调整契约闭环，才允许进入文件修改。
- 涉及一个需求下多页面、跨端同步、结构调整、字段/按钮/状态/文案调整、抽屉同步或复刻改造时，必须先逐页列出：目标页面、当前依据、clone-source、change-type、保留项、新增项、删除/隐藏项、移动/重排项、文案变化、状态与异常、抽屉同步点、禁止新增项、验证方式。
- 契约存在无法从现有页面、文档或用户要求推导的关键项时，必须标为待确认，不得自行补成事实。

## 新增页面与迭代引用闸门

- 新增页面前先判定落位：源目录页面（source-page）或迭代引用（iteration-reference）；未判定不得创建文件。
- 新增业务页面默认落在对应平台/模块源目录；迭代索引只做引用、聚合或快照承载。
- 除非用户明确要求不展示，新增页面必须与所属平台/模块入口的菜单或导航条目在同一任务切片完成，并同步补齐目录分组、承载区或跳转链接、`pageNames` / 面包屑 / URL 参数映射。
- 页面仅被迭代页、聚合页、总导航或搜索索引引用，只能证明「可达」，不算「已注册」；真实注册以源目录入口的菜单或导航闭环为准。
- 新增页面后必须给出迭代引用策略：不挂迭代 / 挂当前迭代 / 待排期 / 冻结快照；无法判断时向用户确认，不得擅自挂入。
- 源目录与迭代目录分组必须与条目标签一致；发现旧目录串期时先报告，不得在普通需求中顺手批量整理。

## 技能自动触发规则

命中 `docs/skills/` 中已定义场景时，必须自动读取并使用对应 skill；不得因为已读本文件就跳过专项 skill。

高频触发：

| 场景 | skill |
| --- | --- |
| 判断需求、方案、规则口径、取舍与风险 | `independent-judgment` |
| 任务画像（0-1 / 纯抽屉 / design over / 既有功能新增 / 微调 / 复刻改造） | `design-task-profiles` |
| 执行页面、抽屉、文档、规则、脚本修改前 | `design-task-preflight` |
| 执行修改完成后、最终回复前 | `implementation-self-audit` |
| 故障排查、页面异常、机检失败、性能问题 | `engineering-diagnose` |
| 先理解模块、页面族、规则体系、脚本链路 | `engineering-zoom-out` |
| 方案评审、需求质询、执行前确认（讨论态不改文件） | `engineering-plan-grill` |
| 拆任务、拆 issue、拆迭代、拆执行包 | `engineering-slice-plan` |
| 实现后的知识收尾（代码/文档/规则/记忆对齐） | `neat-freak` |

- skills 索引与来源声明见 `docs/skills/README.md`。
- 若某个 skill 在当前工具环境不可用，必须在开工声明中说明，并使用最接近的可用 skill 替代；不得静默跳过。

## 交付边界

### 静态原型默认

- 需求原型默认按静态页面交付，仅保留：页面跳转、弹窗打开关闭、抽屉打开关闭、页签切换。
- 除非用户明确要求，不开发：控件互斥禁用、动态显隐、实时联动校验、依赖前序控件结果的状态切换、为演示业务规则额外补写的 mock 逻辑。
- 抽屉里写「支持预览 / 支持删除 / 支持上传」默认是需求表达，不等于必须在设计稿中真实实现。
- 用户已明确「沿用既有逻辑 / 其他按钮静态 / 不需要真实功能」时，不得自行补交互、补提示、补测试项、补验收项。

### 页面与文档边界

- 真实页面正文、业务弹窗、业务抽屉、需求抽屉正文不得混入：需求说明、版本说明、开发备注、测试提示、模拟操作、场景切换器、开发测试按钮。
- 模拟操作与说明统一放在独立控制区（手机模型右侧或 Web 右侧控制轨）。
- 修改可见区域前，必须把本轮「用户可见文案」与「需求/开发说明」分离成剔除清单，剔除清单中的词句不得进入页面 DOM。

### 列表页收口

- 列表页默认只保留：标准筛选区、列表主表、必要操作按钮。
- 禁止再造与正式筛选区重复的快捷筛选按钮、与行内动作重复的顶部功能按钮、演示型状态切换按钮。
- 列表页、详情页、弹窗页的短反馈默认使用 toast；不新增页内提示条替代 toast。

### 说明与抽屉数据源

- 涉及「说明没更新 / 需求抽屉没更新」时必须先定位说明来源：页面静态正文、右侧控制区、页面内联 DocPanel、模块级 `js/docs.js`、页面级 `js/docs/{docId}.js`。
- 单页需求抽屉优先使用页面级增量数据源，避免公共文件 hash 变化连带所有引用页面缓存刷新。
- 修改抽屉数据源后必须检查脚本加载顺序：公共 `doc-builder` → 模块级 `docs.js` → 页面级 `docs/{docId}.js` → `doc-panel.js`。

## 可见页面改动证据闭环

- 声称「页面已变化 / 弹窗已调整 / 入口已生效」的修改，交付前必须完成可见证据闭环：打开真实入口、触发目标按钮、读取目标 DOM 状态、确认用户可见区域。
- 仅运行语法检查、`git diff --check` 或搜索命中，不足以证明页面可见改动生效。
- 用户反馈「没变化 / 还是一样 / 没效果」时，必须立即进入排障态，先诊断实际加载文件、入口路由、缓存版本、遮挡层与 DOM 状态。
- 无法完成可见证据闭环时，只能说「本地代码已改，页面可见效果未验证」。

## 验证与交付

- 窄验证优先：页面改动跑相关机检（`npm run lint:ui` 等）并做浏览器/入口复核；规则与文档改动跑 `git diff --check`。
- 默认不跑全量 `check:all`、`audit:*`、导出类命令，除非用户明确要求或改动面覆盖。
- 部署与上传由使用方自持：本仓库不包含部署配置、服务器地址或上传脚本；部署方式与缓存策略以使用方环境为准。
- 需求或治理任务完成后，区分本轮改动、既有非本轮改动、未跟踪文件与禁止触碰范围；提交范围默认只包含本轮改动。
- 提交信息必须能对应本轮需求或治理目标；不得使用无法追溯的泛化信息。
- 最终回复必须说明：是否已提交、是否已推送、当前分支与远端同步状态；未提交或未推送时说明阻塞原因。

## 新文件与命名准入

- 新增页面、文档、快照、图片、模板、脚本时，先确认所属端、模块、迭代与页面类型，复用现有同类命名风格；无法判断时标为待确认，不自创新目录体系。
- 新增页面优先放入既有平台/模块的 `pages/` 或既有模块目录；新增历史快照必须放入快照目录并满足快照元信息规则。
- 新增文件名应表达业务对象与页面身份，避免「新页面」「测试」「最终版」「临时」「副本」等不可追溯命名。
- 旧目录混乱、命名不规范、入口散落等问题，先输出治理清单与迁移影响分析，再单独立迁移任务。
