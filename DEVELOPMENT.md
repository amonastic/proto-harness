# 页面实现基线

本文件负责页面结构、交互、样式、抽屉实现与复刻改造的通用规则。开工流程、执行边界、授权口径与红线以 `AGENTS.md` 与 `harness/` 为准；结构化机检口径以 `standards/*` 为准。

## 适用范围

适用于本仓库约定的「目录型设计稿仓库」：以一个或多个平台目录（由 `governance.config.json` 声明）承载静态原型页面，配合迭代索引目录做需求聚合。

## 页面类型与导航边界

| 类型 | 判定 | 导航约束 |
| --- | --- | --- |
| `platform-entry` | `{platform}/index.html` | 允许平台级导航（system-nav / 侧栏） |
| `iteration-index` | 迭代目录下的页面 | 有导航，但只做聚合入口，不写业务 |
| `module-page` | `{platform}/{module}/index.html` 及其 `pages/*` | 不得混入平台级导航 |
| `portal-index` | 总导航页、平台总览页 | 不做平台级系统导航 |

- 文件名是 `index.html` 不等于入口页，按页面职责判断。
- 平台归属由 `governance.config.json` 的 `platforms[].key`（web / app / miniapp）决定。

目录型页面返回区约束：

- 除总导航页、迭代索引页与临时设计目录外，目录型 `index.html` 底部默认保留两行返回入口：第一行返回所属平台或上级导航，第二行返回总导航；两行使用统一弱化链接样式。

## 目录入口注册闭环

新增、修复或调整可被开发查看的页面入口时，必须同时满足：

1. 菜单项或模块卡片
2. 页面承载区或跳转 `href`
3. `pageNames` / 面包屑 / URL 参数映射

三者缺一视为入口未完成。不得仅因为页面文件存在、已登记进 `standards/ui-spec.json` 或已被迭代索引引用，就判断入口已闭环。

- 真实注册以源文件所属平台目录的前端菜单或导航为准；迭代页、聚合页、总导航、搜索索引的引用只能证明「可达」。
- 新增菜单条目必须展示已确认所属迭代的完整月份标签（`YYYYMM上` / `YYYYMM下`），并与目录分组、承载页注释及 `pageNames` 保持同一口径；月份归属无依据时不得猜测或省略。
- iframe 型承载页新增入口必须同时检查：`data-page`、页面承载区 `id="page-..."`、`iframe src`、`pageNames`、URL 参数进入逻辑。

## 页面与抽屉实现基线

除临时设计目录外，正式设计页面默认都需要：

- 需求文档抽屉
- CSS 规范抽屉

抽屉实现约定：

- 公共组件：`assets/js/components/doc-panel.js`（提供 `DocPanel.open / close / toggle / autoShow / isOpen`）。
- 数据契约：`window.DocsData[docId] = { title, requirement | requirementHtml, css }`；`requirement` 使用 `assets/js/doc-builder.js` 的结构化 API 生成。
- 抽屉宽度统一 `780px`（见 `standards/doc-spec.json`）；历史窄宽度只作为历史兼容信息。
- Web 页面默认手动打开抽屉（不得自动展开）；App / 小程序页面默认自动展开（`DocPanel.autoShow`）。
- 抽屉关闭按钮必须复用既有成熟样式；残留字符串拼接片段（`+`、`'</tr>'` 等）不得泄漏到正文。

禁止做法：

- 用大范围 utility class 兜正文，或用通配式 `border/background` 规则污染标题与分节
- 只修一个 Tab 导致另一个 Tab 排版失效
- 只写按钮定位、不写按钮视觉外观

## 页面正文边界

以下内容不得混入真实页面正文、业务弹窗、业务抽屉、需求抽屉正文：

- 需求说明、版本说明、本期新增摘要、开发备注、测试提示
- 模拟操作、场景切换器、开发测试按钮、仅设计稿可见的标注

正确放置位置：手机稿放在手机模型右侧独立控制区；Web 稿放在页面右侧独立控制轨。

## 复刻改造规则

复刻改造前必须声明：

- `clone-source`（复刻来源页面或组件）
- `change-type`：`safe-add` / `safe-reorder` / `visual-tighten` / `breaking-change`

以截图作为复刻依据时，还必须声明：截图来源、可直接复刻项、按系统风格适配项、不可凭截图写成事实项、待确认项；截图中的字段、按钮、状态标签、列表列名、表单项与区块顺序必须逐项核对，发生合并或改排必须写清映射关系。

`safe-add` 默认只允许：新增字段、新增标签、紧凑重排但不改语义；不允许顺手新增筛选器、统计条、快捷入口、说明卡片或模块区块。

## 列表页收口

列表页默认只保留：标准筛选区、列表主表、与当前列表直接相关的必要操作按钮。

禁止额外再造：

- 与正式筛选区重复的快捷筛选按钮
- 与列表行内动作或既有筛选能力重复的顶部功能按钮
- 仅为演示筛选结果而新增的快捷入口、统计联动按钮、重复状态切换按钮

判断原则：正式筛选区已能完成筛选，就不要再补快捷筛选；列表现有操作链路已能完成任务，就不要再补重复功能按钮。

`⚡` 标识只用于直接对应接口的页面、区块、按钮或字段来源说明；本地交互、纯前端筛选逻辑与展示规则不得挂 `⚡`。

## 交互反馈默认规则

- 列表页、详情页、弹窗页的短反馈默认使用 toast。
- 非必要不新增页内提示条、反馈盒、提示面板来替代 toast。

## 静态原型默认规则

需求原型默认按静态页面交付，只保留：页面跳转、对话框打开关闭、抽屉打开关闭、页签切换。

除非用户明确要求，不开发：控件互斥禁用、动态显隐、实时联动校验、依赖前序控件结果的状态切换、为演示业务规则额外补写的 mock 逻辑。

## 业务弹层规则

业务对话框不得使用浏览器视口级半透明阻断遮罩，避免打开弹窗后无法点击需求文档入口或滚动已打开的需求抽屉。

默认实现：

```css
.modal-mask {
  position: fixed;
  inset: 0;
  background: transparent;
  pointer-events: none;
}

.modal {
  pointer-events: auto;
}
```

- Web / 小程序 / 迭代索引 / iframe 承载页：弹层可居中，但遮罩必须透明且不拦截非弹窗区域点击。
- 移动端手机稿：弹窗必须限定在手机模型内部，不使用浏览器视口级遮罩。
- 只有用户明确要求遮罩时，才允许半透明遮罩，并说明对抽屉操作的影响。

### 批量导入弹窗复用规则

涉及表格/模板类批量导入的一步式或分步式弹窗时，优先复用仓库或项目内既有的成熟导入弹窗结构；允许修改范围仅限：弹窗标题、模板下载区文案、上传区文案、导入说明区业务文案。不得自创第二套导入弹窗结构、步骤样式或结果样式。

## 文档落位约束

- 页面抽屉优先于独立需求文档文件；普通迭代页面默认 `drawer-first`。
- 大需求、跨多页、0-1 独立模块或用户明确要求时，才使用独立需求文档（`md-required`）。
- 版本标记默认内联到具体字段、规则、边界条目中，格式 `[202604上]`，不靠顶部摘要块替代正文定位。
- 当前迭代的需求抽屉只展示对应本期需求；历史内容留在模块源文档或历史快照中。

## 目录结构与资源接入

标准模块目录：

```text
模块名/
├── index.html
├── pages/
├── js/
├── css/
└── doc/
```

`pages/` 的拆分时机：模块主页面过长、存在独立详情/编辑/配置页、页面间存在明确跳转关系。

资源路径基线（相对页面自身）：

| 层级 | 位置 | assets 路径 |
| --- | --- | --- |
| L0 | 根目录 | `assets/...` |
| L1 | 平台页 | `../assets/...` |
| L2 | 模块页 | `../../assets/...` |
| L3 | `pages/` 子页 | `../../../assets/...` |

## 常见实现约束

- 实现任何布局、交互、组件模式前，先在同平台或同模块目录下搜索成熟页面复用。
- 不允许脱离成熟页面从零自创高风险方案。
- 修改抽屉按钮、关闭按钮、头部、层级时，必须复用既有成熟样式。
- 重写 `innerHTML` 或模板字符串后，必须检查拼接残渣泄漏。
- 修改 HTML 内联 `<script>` 后，必须做语法检查（如 `node --check` 提取检查），避免整段脚本失效。

## 按钮命名规则

- 按钮文案使用稳定、通用的动作命令，不把当前选择值、业务变量或临时状态拼进按钮名。
- 正确示例：`打开格口`、`提交`、`确认`、`确定`、`取消`、`关闭`、`保存`。
- 禁止示例：把具体对象名或状态拼进按钮文案造成同名按钮语义漂移。
